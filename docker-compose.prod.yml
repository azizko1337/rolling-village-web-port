services:
  frontend:
    build:
      context: .
      target: runner
    ports:
      - "3020:3020"
    environment:
      - NODE_ENV=production
    networks:
      - rolling-village-network

networks:
  rolling-village-network:
    driver: bridge
